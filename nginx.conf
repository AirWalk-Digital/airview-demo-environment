events { 
}
http {
  server {
    listen 5000;
    
    location /_api/api/ {
       proxy_pass http://api/;
    }
    location /_api/storage/ {
       proxy_pass http://storage:8080/;
    }
   
    location /{
       proxy_pass http://frontend/;
    }
  }
  }